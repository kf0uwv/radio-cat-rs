# Task Plan — architect

No extraction yet; awaiting ts570d refactor completion and an explicit
go-ahead.

This repository currently holds planning and agent scaffolding only (see
`docs/adr/0001-scope-and-crate-boundaries.md` and the root `CLAUDE.md`). There
is no crate source to plan implementation work against yet. When extraction
is authorized, this file should be replaced with an actual breakdown of the
extraction work across the `cat_framework`, `cat_transport`, and `cat_server`
subagents, gated on `ts570d`'s own refactor status.
