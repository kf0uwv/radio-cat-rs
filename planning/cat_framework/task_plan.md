# Task Plan — cat_framework

No extraction yet; awaiting ts570d refactor completion and an explicit
go-ahead.

`cat-framework` and `cat-client` do not exist as code yet. When extraction is
authorized, this file should record the plan for lifting `ts570d`'s
`framework/src/cat.rs`, `errors.rs`, and the `CatSession`-adjacent client
mechanics into these crates, per
`docs/adr/0001-scope-and-crate-boundaries.md`.
